<h1>Edit Account Details of <%= @customer.name %> </h1>
<%= form_for(@customer, :url => customer_path(@customer), :html => { :method => "put"}) do |f| %>
	<%= render "customers/form", f: f %>
<% end %>		